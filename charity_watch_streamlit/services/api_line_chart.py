import pandas as pd
import plotly.express as px

def build_charity_income_line_chart(df:pd.DataFrame):
    """
    This function plots the data received from the Charity Commission API in a line chart for a selected charity.
    """
